# React Native's dependency graph routinely has mismatched peer ranges between
# expo, react-native-web and react-dom. npm 7+ strict peers block installs that
# work fine in practice; Expo's own tooling assumes this setting.
legacy-peer-deps=true
